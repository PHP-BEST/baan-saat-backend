npm test || {
  echo "==================================================================="
  echo "❌ You can't commit your code since there are failed tests! ❌"
  echo "==================================================================="
  exit 1
}